////	JavaScript Commands	////
	//
	//	Alert()
	//	document.write()
	//	console.log()
	//	isNaN()
	//	throw new Error()

//// Alert()
	// Built in command, opens a dialog box and displays a message
	alert("Alert from Treehouse");

//// document.write()
	// Allow us to write content on a webpage
	document.write("<h1>Welcome to Javascript Basics</h1>")

//// console.log()
	// Allow us to print log information while a program runs
	// Can pass multiple strings on console seperated by commas 
	// Example: variable, ': ', arr[3] 
	console.log("Program complete");

//// prompt()
	// Captures visitor input from a dialog box
	prompt('What is your name?');
	// We can store that input in a variable like this:
	var visitorName = prompt('What is your name?');

//// isNaN()
	// Check if a value is Not a Number

//// throw new Error()
	// Throws a custom error

//// typeof
	// Checks the typo of data and return a word (e.g. string)
	console.log( typeof xhr.responseText );